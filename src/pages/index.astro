---
import { sanityClient } from "sanity:client";

export const prerender = false;

// 1. Datos de Sanity
const mainCase = await sanityClient.fetch(`*[_type == "caseStudy" && slug.current == "clinica-dental-sonrisas-valencia"][0]`);
const clusterPosts = await sanityClient.fetch(`*[_type == "post"] | order(_createdAt desc)`);

// 2. Mapeo de Iconos exacto para tus 6 posts pilares + adicionales
const getIcon = (title) => {
  const t = title.toLowerCase();
  if (t.includes('reseñas con ia')) return '🤖';
  if (t.includes('google maps') || t.includes('local pack')) return '📍';
  if (t.includes('business messages') || t.includes('chat')) return '💬';
  if (t.includes('errores') || t.includes('suspensiones')) return '⚠️';
  if (t.includes('estadísticas') || t.includes('insights')) return '📊';
  if (t.includes('guía pilar') || t.includes('conceptos generales')) return '🏛️';
  if (t.includes('link building')) return '🔗';
  if (t.includes('audit')) return '🔍';
  return '📄'; // Icono por defecto
};
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Local SEO Architect | Home</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans">

    <header class="relative bg-blue-900 text-white py-20 px-6 overflow-hidden">
      <div class="max-w-5xl mx-auto text-center relative z-10">
        <span class="bg-blue-500/30 text-blue-200 px-3 py-1 rounded-full text-xs font-bold tracking-widest uppercase border border-blue-500/50 mb-6 inline-block">
            Caso de Éxito
        </span>
        <h1 class="text-3xl md:text-5xl font-extrabold mb-8 leading-tight">
          Cómo duplicamos las citas de una Clínica Dental en el centro de Valencia en 4 meses
        </h1>
        <div class="flex flex-col md:row justify-center gap-4">
          <a href={`/casos-de-exito/${mainCase?.slug?.current}`} class="bg-blue-600 hover:bg-blue-500 text-white px-8 py-3 rounded-lg font-bold transition shadow-lg">
            Ver Resultado Detallado
          </a>
          <a href="/casos-de-exito" class="bg-white/10 hover:bg-white/20 text-white border border-white/30 px-8 py-3 rounded-lg font-bold backdrop-blur-sm transition">
            Todos los Casos
          </a>
        </div>
      </div>
    </header>

    <main class="max-w-7xl mx-auto py-16 px-6">
      <div class="mb-12 border-l-4 border-blue-600 pl-4">
        <h2 class="text-3xl font-bold">Base de Conocimiento</h2>
        <p class="text-gray-600">Estrategias y SEO Local 2026</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        {clusterPosts.map((post) => (
          <a href={`/blog/${post.slug.current}`} class="group bg-white p-6 rounded-2xl shadow-sm border border-gray-100 hover:shadow-xl hover:border-blue-500 transition-all flex flex-col h-full">
            <div class="text-4xl mb-4 transform group-hover:scale-110 transition-transform">
                {getIcon(post.title)}
            </div>
            <h3 class="text-lg font-bold mb-4 text-gray-800 group-hover:text-blue-600 flex-grow">
              {post.title}
            </h3>
            <div class="text-blue-600 font-bold text-xs flex items-center mt-4 tracking-widest">
              LEER MÁS <span class="ml-2 group-hover:translate-x-1 transition-transform">→</span>
            </div>
          </a>
        ))}
      </div>
    </main>

  </body>
</html>