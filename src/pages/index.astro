---
import { sanityClient } from "sanity:client";

export const prerender = false;

// 1. Traemos los clústeres con todos sus campos
const clusters = await sanityClient.fetch(`*[_type == "cluster"] | order(_createdAt asc)`);
const mainCase = await sanityClient.fetch(`*[_type == "caseStudy" && slug.current == "clinica-dental-sonrisas-valencia"][0]`);

const getIcon = (title) => {
  const t = title?.toLowerCase() || '';
  if (t.includes('business profile')) return '🏢';
  if (t.includes('link building')) return '🔗';
  if (t.includes('factores')) return '📈';
  if (t.includes('herramientas')) return '🔍';
  if (t.includes('ai') || t.includes('sge')) return '🧠';
  return '📁';
};
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Local SEO Architect</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans">

    <header class="bg-blue-900 text-white py-20 px-6 text-center relative">
      <nav class="absolute top-8 right-8">
        <a href="/contactame" class="text-blue-200 hover:text-white font-bold text-sm uppercase tracking-widest transition">Contacto</a>
      </nav>
      <div class="max-w-4xl mx-auto">
        <h1 class="text-4xl font-extrabold mb-4">Local SEO Knowledge Base</h1>
      </div>
    </header>

    <main class="max-w-7xl mx-auto py-24 px-6">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {clusters.map((item) => {
          
          // --- LÓGICA DE DETECCIÓN AGRESIVA ---
          
          // Buscamos la descripción probando todos estos nombres posibles que usa Sanity
          const desc = item.seoDescription || 
                       item.descriptionSEO || 
                       item.description || 
                       item.descripcion || 
                       item.resumen ||
                       // Si nada de eso existe, buscamos CUALQUIER texto largo en el objeto
                       Object.values(item).find(v => typeof v === 'string' && v.length > 40 && v !== item.title) ||
                       "Optimización avanzada para negocios locales.";

          // Buscamos el Alt Text dentro de cualquier objeto de imagen
          const img = item.mainImage || item.imagenPortada || item.image || {};
          const altText = img.alt || img.altText || img.textAlt || img.caption || item.title;

          return (
            <a href={`/${item.slug?.current}`} class="group bg-white p-10 rounded-2xl shadow-sm border border-gray-100 hover:shadow-xl transition-all flex flex-col items-start">
              <div class="text-5xl mb-6" title={altText}>{getIcon(item.title)}</div>
              <h3 class="text-xl font-bold text-gray-800 group-hover:text-blue-600 mb-4">{item.title}</h3>
              
              <p class="text-gray-500 mb-8 leading-relaxed line-clamp-3">
                {desc}
              </p>

              <div class="mt-auto text-blue-600 font-bold text-xs uppercase tracking-widest">
                Explorar →
              </div>
            </a>
          );
        })}
      </div>
    </main>

    <footer class="py-12 border-t text-center text-gray-400 text-sm">
      <p>&copy; 2026 Local SEO Architect</p>
    </footer>
  </body>
</html>
