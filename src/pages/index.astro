---
import { sanityClient } from "sanity:client";

export const prerender = false;

// 1. Consultas a Sanity
const mainCase = await sanityClient.fetch(`*[_type == "caseStudy" && slug.current == "clinica-dental-sonrisas-valencia"][0]`);
const clusters = await sanityClient.fetch(`*[_type == "cluster"] | order(_createdAt asc)`);

// 2. Mapeo de Iconos
const getIcon = (title) => {
  const t = title.toLowerCase();
  if (t.includes('link building')) return '🔗';
  if (t.includes('factores de ranking')) return '📈';
  if (t.includes('herramientas')) return '🔍';
  if (t.includes('sge') || t.includes('ai')) return '🧠';
  if (t.includes('social proof') || t.includes('live')) return '📱';
  if (t.includes('lead magnet')) return '🗺️';
  if (t.includes('web local')) return '💻';
  if (t.includes('nichos')) return '💰';
  if (t.includes('business profile')) return '🏢';
  return '📁';
};
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Local SEO Architect | Home</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 text-gray-900">

    <header class="bg-blue-900 text-white py-20 px-6 text-center">
      <div class="max-w-4xl mx-auto">
        <span class="bg-blue-600 text-xs font-bold px-3 py-1 rounded mb-6 inline-block uppercase">Caso de Éxito</span>
        <h1 class="text-3xl md:text-5xl font-extrabold mb-8 leading-tight">
          Cómo duplicamos las citas de una Clínica Dental en el centro de Valencia en 4 meses
        </h1>
        <div class="flex flex-col sm:flex-row justify-center gap-4">
          <a href={`/casos-de-exito/${mainCase?.slug?.current}`} class="bg-blue-600 px-8 py-3 rounded-md font-bold hover:bg-blue-500 transition">Ver Caso de Éxito</a>
          <a href="/casos-de-exito" class="bg-white text-blue-900 px-8 py-3 rounded-md font-bold hover:bg-gray-100 transition">Ver todos los casos</a>
        </div>
      </div>
    </header>

    <main class="max-w-7xl mx-auto py-16 px-6">
      <div class="mb-12">
        <h2 class="text-3xl font-bold text-gray-800 uppercase tracking-tighter">Our Knowledge Base: Content Cluster</h2>
        <p class="text-gray-500">Explora nuestros clústeres de expertos en SEO Local.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {clusters.map((item) => (
          /* CAMBIO CLAVE: Se elimina /cluster/ de la URL */
          <a href={`/${item.slug.current}`} class="group bg-white p-8 rounded-xl shadow-sm border border-gray-100 hover:shadow-xl transition-all flex flex-col items-start">
            <div class="text-5xl mb-6">{getIcon(item.title)}</div>
            <h3 class="text-xl font-bold text-gray-800 group-hover:text-blue-600 mb-4 transition-colors">
              {item.title}
            </h3>
            <div class="mt-auto text-blue-600 font-bold text-sm flex items-center">
              EXPLORAR <span class="ml-2">→</span>
            </div>
          </a>
        ))}
      </div>
    </main>

  </body>
</html>