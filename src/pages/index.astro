---
import { sanityClient } from "sanity:client";

export const prerender = false;

const mainCase = await sanityClient.fetch(`*[_type == "caseStudy" && slug.current == "clinica-dental-sonrisas-valencia"][0]`);
const clusters = await sanityClient.fetch(`*[_type == "cluster"] | order(_createdAt asc)`);

const getIcon = (title) => {
  const t = title?.toLowerCase() || '';
  if (t.includes('link building')) return '🔗';
  if (t.includes('factores')) return '📈';
  if (t.includes('herramientas')) return '🔍';
  if (t.includes('sge') || t.includes('ai')) return '🧠';
  if (t.includes('social proof')) return '📱';
  if (t.includes('business profile')) return '🏢';
  return '📁';
};
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Local SEO Architect | Estrategia</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans">

    <header class="bg-blue-900 text-white py-20 px-6 text-center relative">
      <nav class="absolute top-8 right-8">
        <a href="/contactame" class="text-blue-200 hover:text-white font-bold text-sm uppercase tracking-widest transition">Contacto</a>
      </nav>
      <div class="max-w-4xl mx-auto">
        <span class="bg-blue-600 text-xs font-bold px-3 py-1 rounded mb-6 inline-block uppercase">Caso de Éxito</span>
        <h1 class="text-3xl md:text-5xl font-extrabold mb-8 leading-tight">Clínica Dental Sonrisas Valencia: Duplicando citas</h1>
        <a href={`/casos-de-exito/${mainCase?.slug?.current}`} class="bg-blue-600 px-8 py-3 rounded-md font-bold hover:bg-blue-500 transition">Ver Caso de Éxito</a>
      </div>
    </header>

    <main class="max-w-7xl mx-auto py-24 px-6">
      <div class="mb-16">
        <h2 class="text-4xl font-black text-gray-900 uppercase tracking-tighter italic">Knowledge Base</h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {clusters.map((item) => {
          // LÓGICA CORREGIDA: Buscamos campos de texto de longitud media (descripciones)
          const allStrings = Object.values(item).filter(v => typeof v === 'string');
          const desc = item.description || item.seoDescription || item.descriptionSEO || 
                       allStrings.find(v => v.length > 35 && v.length < 400 && v !== item.title) || 
                       "Guía estratégica de posicionamiento local.";

          const img = item.mainImage || item.imagenPortada || item.image || {};
          const alt = img.alt || img.altText || img.textAlt || item.title;

          return (
            <a href={`/${item.slug?.current}`} class="group bg-white p-10 rounded-2xl shadow-sm border border-gray-100 hover:shadow-xl transition-all flex flex-col items-start">
              <div class="text-5xl mb-6" title={alt}>{getIcon(item.title)}</div>
              <h3 class="text-2xl font-bold text-gray-800 group-hover:text-blue-600 mb-4 uppercase">{item.title}</h3>
              <p class="text-gray-500 mb-8 leading-relaxed line-clamp-3 italic">{desc}</p>
              <div class="mt-auto text-blue-600 font-bold text-xs tracking-widest uppercase">Explorar →</div>
            </a>
          );
        })}
      </div>
    </main>

    <footer class="py-12 border-t text-center text-gray-400 text-sm uppercase tracking-widest">
      <p>&copy; 2026 Local SEO Architect</p>
    </footer>
  </body>
</html>
