---
import { sanityClient } from "sanity:client";

export const prerender = false;

// 1. Traemos el Caso de Éxito principal
const mainCase = await sanityClient.fetch(`*[_type == "caseStudy" && slug.current == "clinica-dental-sonrisas-valencia"][0]`);

// 2. Traemos TODOS los posts del clúster (ajustamos el filtro para no perder ninguno)
// Quitamos el límite de [0...10] si tienes más o queremos asegurar que salgan todos los tipos
const clusterPosts = await sanityClient.fetch(`*[_type == "post"] | order(_createdAt desc)`);

// 3. Mapeo de Iconos mejorado para cubrir más categorías
const getIcon = (title) => {
  const t = title.toLowerCase();
  if (t.includes('reseñas') || t.includes('ia')) return '🤖';
  if (t.includes('maps') || t.includes('local pack')) return '📍';
  if (t.includes('enlaces') || t.includes('link')) return '🔗';
  if (t.includes('mensajes') || t.includes('chat')) return '💬';
  if (t.includes('guía') || t.includes('business profile')) return '🏢';
  if (t.includes('estadísticas') || t.includes('insights')) return '📊';
  if (t.includes('errores') || t.includes('suspensiones')) return '⚠️';
  return '📝';
};
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Local SEO Architect | Inicio</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 text-gray-900">

    <nav class="p-4 bg-white border-b">
      <a href="/" class="text-blue-600 font-bold hover:underline">← Volver al Inicio</a>
    </nav>

    <header class="bg-blue-900 text-white py-24 px-6 text-center relative">
      <div class="max-w-4xl mx-auto">
        <h1 class="text-4xl md:text-6xl font-black mb-8 leading-tight">
          Cómo duplicamos las citas de una Clínica Dental en Valencia
        </h1>
        <div class="flex flex-col md:flex-row justify-center gap-4">
          <a href={`/casos-de-exito/${mainCase?.slug?.current}`} class="bg-blue-600 px-8 py-4 rounded-xl font-bold hover:bg-blue-500">Ver Caso de Éxito</a>
          <a href="/casos-de-exito" class="bg-white text-blue-900 px-8 py-4 rounded-xl font-bold">Ver todos los Casos</a>
        </div>
      </div>
    </header>

    <main class="max-w-7xl mx-auto py-20 px-6">
      <h2 class="text-3xl font-bold mb-10 text-center text-slate-800">Nuestro Clúster de Contenidos</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        {clusterPosts.map((post) => (
          <a href={`/blog/${post.slug.current}`} class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 hover:shadow-xl transition-all flex flex-col">
            <div class="text-4xl mb-4">{getIcon(post.title)}</div>
            <h3 class="font-bold text-xl mb-4 text-slate-800 flex-grow">{post.title}</h3>
            <span class="text-blue-600 font-bold text-sm">LEER MÁS →</span>
          </a>
        ))}
      </div>
    </main>

  </body>
</html>