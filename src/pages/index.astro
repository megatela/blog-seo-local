---
import { sanityClient } from "sanity:client";

export const prerender = false;

// 1. Datos de Sanity
const mainCase = await sanityClient.fetch(`*[_type == "caseStudy" && slug.current == "clinica-dental-sonrisas-valencia"][0]`);
const clusterPosts = await sanityClient.fetch(`*[_type == "post"] | order(_createdAt desc) [0...10]`);

// 2. Mapeo de Iconos
const getIcon = (title) => {
  const t = title.toLowerCase();
  if (t.includes('reseñas') || t.includes('ia')) return '🤖';
  if (t.includes('guía') || t.includes('profile')) return '📍';
  if (t.includes('link')) return '🔗';
  return '📝';
};
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Local SEO Architect | Resultados Reales</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans">

    <section class="relative bg-blue-900 text-white py-20 px-6">
      <div class="max-w-6xl mx-auto text-center relative z-10">
        <p class="uppercase tracking-widest text-sm font-bold text-blue-300 mb-4">Caso de Éxito Destacado</p>
        <h1 class="text-3xl md:text-5xl font-extrabold mb-8 leading-tight">
          Cómo duplicamos las citas de una Clínica Dental en el centro de Valencia en 4 meses
        </h1>
        <div class="flex flex-col md:flex-row justify-center gap-4">
          <a href={`/casos-de-exito/${mainCase?.slug?.current}`} class="bg-blue-600 hover:bg-blue-500 text-white px-8 py-3 rounded-md font-bold transition">
            Ver Resultado Detallado
          </a>
          <a href="/casos-de-exito" class="bg-white text-blue-900 px-8 py-3 rounded-md font-bold hover:bg-gray-100 transition">
            Todos los Casos de Éxito
          </a>
        </div>
      </div>
      <div class="absolute inset-0 opacity-10 bg-grid-white/[0.2] pointer-events-none"></div>
    </section>

    <main class="max-w-7xl mx-auto py-16 px-6">
      <div class="mb-12">
        <h2 class="text-3xl font-bold mb-2">Base de Conocimiento</h2>
        <p class="text-gray-600">Explora nuestro clúster de expertos en SEO Local.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
        {clusterPosts.map((post) => (
          <a href={`/blog/${post.slug.current}`} class="group bg-white p-6 rounded-xl shadow-md hover:shadow-2xl transition-all border border-gray-100 flex flex-col h-full">
            <div class="text-4xl mb-4">{getIcon(post.title)}</div>
            <h3 class="text-xl font-bold mb-4 group-hover:text-blue-600 flex-grow">
              {post.title}
            </h3>
            <div class="text-blue-600 font-bold text-sm inline-flex items-center mt-4">
              LEER ARTÍCULO <span class="ml-2">→</span>
            </div>
          </a>
        ))}
      </div>
    </main>

  </body>
</html>