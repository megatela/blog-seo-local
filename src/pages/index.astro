---
import { sanityClient } from "sanity:client";

export const prerender = false;

// 1. Consultas a Sanity
const mainCase = await sanityClient.fetch(`*[_type == "caseStudy" && slug.current == "clinica-dental-sonrisas-valencia"][0]`);
const clusters = await sanityClient.fetch(`*[_type == "cluster"] | order(_createdAt asc)`);

// 2. Mapeo de Iconos
const getIcon = (title) => {
  const t = title?.toLowerCase() || '';
  if (t.includes('link building')) return '🔗';
  if (t.includes('factores')) return '📈';
  if (t.includes('herramientas')) return '🔍';
  if (t.includes('sge') || t.includes('ai')) return '🧠';
  if (t.includes('business profile')) return '🏢';
  return '📁';
};
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Local SEO Architect | Estrategia de Posicionamiento</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans">

    <nav class="fixed top-0 w-full z-50 bg-white/80 backdrop-blur-md border-b border-gray-100 py-4 px-8 flex justify-between items-center">
      <a href="/" class="font-black text-xl tracking-tighter uppercase">Local <span class="text-blue-600">Architect</span></a>
      <a href="/contactame" class="bg-blue-600 text-white px-6 py-2 rounded-full font-bold text-sm hover:bg-slate-900 transition-all shadow-lg shadow-blue-200">
        Trabajemos Juntos
      </a>
    </nav>

    <header class="bg-slate-900 text-white pt-40 pb-24 px-6 text-center relative overflow-hidden">
      <div class="max-w-4xl mx-auto relative z-10">
        <span class="bg-blue-600 text-[10px] font-black px-4 py-1 rounded-full mb-8 inline-block uppercase tracking-[0.2em]">Caso de Éxito Real</span>
        
        <h1 class="text-4xl md:text-6xl font-black mb-10 leading-none tracking-tighter uppercase">
          Dominando el Local Pack: <br/> <span class="text-blue-400 italic">Caso Clínica Dental</span>
        </h1>

        <a href={`/casos-de-exito/${mainCase?.slug?.current}`} class="inline-block bg-blue-600 text-white px-10 py-4 rounded-full font-bold hover:bg-blue-700 transition-all uppercase text-sm tracking-widest shadow-xl">
          Ver Estrategia Completa
        </a>
      </div>
    </header>

    <main class="max-w-7xl mx-auto py-32 px-6">
      <div class="mb-20">
        <h2 class="text-5xl font-black text-gray-900 uppercase tracking-tighter leading-none">Knowledge Base</h2>
        <p class="text-xl text-gray-400 mt-4 font-light italic">Estrategias de SEO Local para dominar tu mercado.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
        {clusters.map((item) => {
          // Lógica de detección de descripción SEO (texto de longitud media)
          const allStrings = Object.values(item).filter(v => typeof v === 'string');
          const desc = item.description || item.seoDescription || 
                       allStrings.find(v => v.length > 35 && v.length < 400 && v !== item.title) || 
                       "Guía técnica sobre optimización y visibilidad local.";

          return (
            <a href={`/${item.slug?.current}`} class="group bg-white p-12 rounded-[2.5rem] shadow-sm border border-gray-100 hover:shadow-2xl hover:-translate-y-2 transition-all duration-500 flex flex-col items-start">
              <div class="text-6xl mb-10 transform group-hover:scale-110 transition-transform duration-500">{getIcon(item.title)}</div>
              <h3 class="text-2xl font-black text-gray-800 group-hover:text-blue-600 mb-6 uppercase tracking-tight leading-tight transition-colors">{item.title}</h3>
              <p class="text-gray-500 mb-10 leading-relaxed italic font-light line-clamp-3 text-lg">{desc}</p>
              <div class="mt-auto text-blue-600 font-black text-xs tracking-[0.3em] uppercase flex items-center">
                Entrar al clúster <span class="ml-2 transform group-hover:translate-x-3 transition-transform">→</span>
              </div>
            </a>
          );
        })}
      </div>
    </main>

    <section class="bg-blue-600 py-32 px-6 text-center text-white">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-5xl md:text-7xl font-black mb-8 tracking-tighter uppercase leading-none">
          ¿Tu negocio no aparece <br/> en Google Maps?
        </h2>
        <p class="text-xl md:text-2xl text-blue-100 mb-12 font-light max-w-2xl mx-auto">
          Analizamos tu presencia local y diseñamos el mapa de ruta para tu crecimiento.
        </p>
        <a href="/contactame" class="inline-block bg-white text-blue-600 font-black px-12 py-5 rounded-full hover:bg-slate-900 hover:text-white transition-all duration-300 uppercase tracking-widest text-sm shadow-xl">
          Solicitar Auditoría Local →
        </a>
      </div>
    </section>

    <footer class="py-20 bg-white text-center text-gray-400 text-[10px] font-bold uppercase tracking-[0.5em]">
      <p>&copy; 2026 Local SEO Architect</p>
    </footer>

  </body>
</html>
