---
import { sanityClient } from "sanity:client";

export const prerender = false;

// 1. Consulta a Sanity: Traemos todo el objeto para inspeccionar sus campos
const mainCase = await sanityClient.fetch(`*[_type == "caseStudy" && slug.current == "clinica-dental-sonrisas-valencia"][0]`);
const clusters = await sanityClient.fetch(`*[_type == "cluster"] | order(_createdAt asc)`);

// 2. Mapeo de Iconos
const getIcon = (title) => {
  const t = title?.toLowerCase() || '';
  if (t.includes('link building')) return '🔗';
  if (t.includes('factores de ranking')) return '📈';
  if (t.includes('herramientas')) return '🔍';
  if (t.includes('sge') || t.includes('ai')) return '🧠';
  if (t.includes('social proof') || t.includes('live')) return '📱';
  if (t.includes('lead magnet')) return '🗺️';
  if (t.includes('web local')) return '💻';
  if (t.includes('nichos')) return '💰';
  if (t.includes('business profile')) return '🏢';
  return '📁';
};
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Local SEO Architect | Home</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans">

    <header class="bg-blue-900 text-white py-24 px-6 text-center relative overflow-hidden">
      <nav class="absolute top-8 right-8 z-10">
        <a href="/contactame" class="text-blue-200 hover:text-white font-bold text-sm uppercase tracking-widest transition">
          Contacto
        </a>
      </nav>

      <div class="max-w-4xl mx-auto relative z-10">
        <span class="bg-blue-600 text-xs font-bold px-3 py-1 rounded mb-6 inline-block uppercase tracking-tighter">Caso de Éxito</span>
        <h1 class="text-3xl md:text-5xl font-extrabold mb-8 leading-tight">
          Cómo duplicamos las citas de una Clínica Dental en el centro de Valencia en 4 meses
        </h1>
        <div class="flex flex-col sm:row justify-center gap-4">
          <a href={`/casos-de-exito/${mainCase?.slug?.current}`} class="bg-blue-600 px-8 py-3 rounded-md font-bold hover:bg-blue-500 transition">Ver Caso de Éxito</a>
        </div>
      </div>
    </header>

    <main class="max-w-7xl mx-auto py-24 px-6">
      <div class="mb-16">
        <h2 class="text-4xl font-black text-gray-900 uppercase tracking-tighter italic">Knowledge Base</h2>
        <p class="text-xl text-gray-500">Explora nuestros clústeres de expertos en SEO Local.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {clusters.map((item) => {
          
          // --- LÓGICA DE DETECCIÓN AUTOMÁTICA ---
          
          // 1. Buscamos la descripción: cualquier campo de texto largo (más de 30 carac.) que no sea el título
          const allStringValues = Object.values(item).filter(v => typeof v === 'string');
          const detectedDesc = allStringValues.find(v => v.length > 30 && v !== item.title) || "Estrategia experta de posicionamiento local.";

          // 2. Buscamos el Alt Text: exploramos los objetos de imagen (mainImage, imagenPortada, etc.)
          const imageObject = item.mainImage || item.imagenPortada || item.image || {};
          const detectedAlt = imageObject.altText || imageObject.alt || imageObject.textAlt || item.title || "SEO Local";

          return (
            <a href={`/${item.slug?.current}`} class="group bg-white p-10 rounded-2xl shadow-sm border border-gray-100 hover:shadow-xl transition-all duration-300 flex flex-col items-start relative">
              <div class="text-5xl mb-6" title={detectedAlt}>
                {getIcon(item.title)}
              </div>
              
              <h3 class="text-2xl font-bold text-gray-800 group-hover:text-blue-600 mb-4 transition-colors uppercase tracking-tight">
                {item.title}
              </h3>

              <p class="text-gray-500 mb-8 leading-relaxed line-clamp-3">
                {detectedDesc}
              </p>

              <div class="mt-auto text-blue-600 font-bold text-xs tracking-widest uppercase flex items-center">
                Explorar Clúster <span class="ml-2 transform group-hover:translate-x-2 transition-transform">→</span>
              </div>
            </a>
          );
        })}
      </div>
    </main>

    <section class="bg-white py-32 px-6 border-t border-gray-100 text-center">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-4xl md:text-6xl font-black text-gray-900 mb-8 tracking-tighter uppercase">
          ¿Listo para dominar <br/> el Local Pack?
        </h2>
        <p class="text-xl text-gray-500 mb-12 max-w-2xl mx-auto">
          Hablemos de tu estrategia. Sin rodeos, solo resultados medibles.
        </p>
        <a 
          href="/contactame" 
          class="inline-block bg-blue-600 text-white font-black px-12 py-5 rounded-full hover:bg-blue-700 hover:shadow-2xl transition-all duration-300 uppercase tracking-widest text-sm"
        >
          Empezar Ahora →
        </a>
      </div>
    </section>

    <footer class="py-16 border-t border-gray-50 text-center text-gray-400 text-xs tracking-[0.3em] uppercase">
      <p>&copy; 2026 Local SEO Architect. Todos los derechos reservados.</p>
    </footer>

  </body>
</html>